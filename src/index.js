import React from 'react';
import ReactDOM from 'react-dom/client';
import MillerFamilyDashboard from './App';
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
<React.StrictMode>
<MillerFamilyDashboard />
</React.StrictMode>
);
